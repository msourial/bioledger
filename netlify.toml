[build]
  base = "artifacts/bio-ledger"
  publish = "dist/public"
  # No build command - files are pre-built locally

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
